todo:
* when not running as admin, and switching from task manager (higher integrity (0x3000 > 0x2000)) to a normal one like cmd.exe window even tho i held down winkey it won't trigger gesture obviously because when LMB was sensed the target wasn't focused yet so it was still higher integrity so windows didn't send us the event, but once that cmd.exe window is focused re-LMB-ing did sense winkey was down and triggered event, so my question/goal is that maybe we can actually during mouse move/drag with winkey+LMB down on cmd.exe (while just came from task manager being focused) can be used to detect if those keys are held down and trigger the gesture even tho it didn't see the LMB down event. So basically we could track the mouse-move event and see if those winkey and LMB are down and then trigger the same logic from LMB is down(then checking if winkey is down, then enabling gesture), except ofc, it might be expensive/stuttery to do, and also we can't trigger it again if it's already triggered/in-progress!
 * this makes me wonder if I'm preventing the retriggering of the gesture if it's already in progress, ie. if two LMB down events come thru(for wtw reasons!), the first triggers the gesture, the second does what? re-triggers it? updates it? or just does nothing because it's already in progress?! should be the latter.

* ShouldThrottle doesn't seem to be hit anymore now, but in theory it still can be, thus that FIXME in drainMoveChannelCoalesced() must be applied to ensure it won't drop/ignore any resizes/moves because likely since they're coalesced it's the only resize/move that's being done for that window, unless ... resize/move itself takes so little time and mouse moves so fast that it's doing more than the rate that ShouldThrottle expects it to be under, in which case it would trigger and probably wouldn't see any difference if so, hmm.

* incredible amount of errcheck (116 of them) ie. returned errors not checked, most in proc*.Call() calls, now they're hidden behind(because of) WinResult (the return from .Call()) !

* do I want to bring focused to front(on drag/move gesture) no matter what (or check if not in front first?) this way, if I winkey+MMB (send it to back) then winkey+click it to drag/move it, it won't remain in the back it will be brought to front. But this should be a systray on/off setting.
* fix this: i press/hold shift then winkey (in this order) them MMB click, then release shift (still holding winkey) and it pops up the start menu! it shouldn't (note that i'm already using Rshift as the workaround for not opening Start menu)
* do winkey+shift+MMB over the non-focused window bring it into focus and thus to top, just like it does for the currently focused window currently, but now we can use the thread-attach method of focusing a window (and maybe use the LMB click fallback here too?) and thus can bring it to zorder top afterwards! So then it removes bring only currently focused window to top which was on same key combo.
* make an option which when on then when currently focused window is fullscreen or borderless fullscreen (if detectable ofc) then don't try to use the gestures ie. bypass the code checking for them, so for example: winkey held down code won't activate, this way, any kind of theoretical slowdown on input(due to calls to getasyncstate or saving/reading atomic bools) will be removed. - it's more expensive than just checking for keys to activate, if I do it in the same place (in hooks), but maybe if I do it in some specific on focus hook then it would work.
* re: focus the drag-moved window first, "Your Current Method: Injected LMB avoids attachment(to target window's thread) but risks UI interactions (e.g., clicking a button starts an action). Attachment + SetForegroundWindow is cleaner for focus-only, no click side effects.", but this means the hooks must be in their own thread to not lock things up. Kept so far as 1 thread and seems to be working, but unclear how correct this is with respect to potential hangs due to target window hanging.
* could maybe check if "crossing DPI monitors mid-drag (system handles)" messes up the coords during drag, for manual or native move. If so find a fix.
* check if it's bad to deinit() in the recovered panic, i remember some AI saying it's bad in the past, but not why.
* add option whether to focus window while the resize is happening/started

notmybug:
//* find out if my reordering of windows via winkey+MMB (sends to back) is why sometimes when a window exits the focus isn't to prev. focused window but to another! or is this exclusively due to tcmd ran as admin after a copy to make it succeed. This is a win11 bug: it happens even without my program running at all: so i restart win11, login, start tcmd, start firefox, LMB whatever else first(or not) then LMB firefox to get it focused, LMB tcmd, start a.bat that does 'pause' from tcmd which makes it gain focus as a cmd.exe terminal window, i press 'e' key because terminal opened and is focused and 'pause' waits for key before exits, it goes away(terminal window disappears) and firefox gets focused instead of tcmd!


done:
//done bycoalescing//* resizing tcmd window is slow/smooth possibly due to too many resize messages sent to it and it taking its time to paint, so basically it looks like it's lagging, smoothly, behind the mouse pointer.
  * make adaptive-resizing for a window like tcmd whereby measured delay compared to amount of resize events we sent it should cause us to recompute the rate at which we can send them thus we can lower it(from the normal)(but never increase it) and remember it (in activeSession?) until a different hwnd is targeted. This will avoid queuing events like 17 events queued, because we don't intend to send more than the target hwnd can handle. Of course there should be a minimum though, maybe 1 resize per second, the minimum?!
  * this ^ should probably only happen when Async Window Resizing is false (the default) else we can't measure that delay between send-resize msg and it finishing the resize event.
  * when that ^ is done, test it on the Find(in progress) tiny window of regedit, which lags for 2 sec sometimes, this will hit the 1 resize minimum. Which makes me wonder, should we snap back to the normal amount of resize requests sent to it if we see it can now handle it? because this Find(while it searches for a string, ie. find is in progress) window of regedit will in fact stutter/freeze sometimes but then be back to normal. So it's unclear how we should handle this case, maybe only down-regulate if the target constantly lags in repaint/resizes ?
//* when we hit this log msg "did a resize but the overlay wasn't shown due to gesture wasn't in effect anymore (thus overlay wouldn't have been cleaned up)" it's in wndProc thread in handleActualMoveOrResize, so we're not showing or updating the overlay which shows size of window during resizing because since the gesture isn't in effect the overlay was torn down?! actually it was only hideOverlay() so hidden, but maybe the question is, shouldn't the handleActualMoveOrResize itself do the showing and hiding of it ? well no because winkey+RMB after a drag (thus at least 1x resize happend) while holding both down still should still show the overlay, so we can't have handleActualMoveOrResize() itself do any hiding, unless maybe it's using some way to sense if the gesture's still in effect and it's in effect for the potentially queued resize it's currently processing and not for a new window's resize which was queued but it's not the currently processed one... Now renamed to "did a resize but the overlay wasn't updated/shown due to gesture wasn't in effect anymore." and considering not showing at all. Gemini agrees with the latter: https://gemini.google.com/app/0d1f3eb0af722304
//* wait for hook thread to finish before continuing to deinit() or primary_defer() in main(), else it wouldn't have finished unhooking before main() os.Exit()s
//* do these fixes for v0.3.1 (todo: done: 2,5 invalid: 1,3,4): https://gemini.google.com/app/55d72e14b36903b3 more details on "1. CRITICAL: //go:uintptrescapes is Broken by Interfaces and Variadics" https://grok.com/project/9be35644-9f16-4f41-860c-bc02a0ff78f5?tab=conversations&chat=6f8553ee-adb0-4817-8de5-3eb2b2286dae and Claude Sonnet 5 Extra Thinking agrees with me that's a non-issue! Only Gemini and Grok are dummies :P
//* maybe I should do the 2 SetCapture(s) at the moment the resize actually happens, tho it has to be done only one time (the first time) instead of doing it from mouseProc (via sendmessage to wndProc) and (2nd case) from RMB button down.
//* extract constants like EVENT_SYSTEM_FOREGROUND
//* fix some big issues in v0.1.0 (done: 1,2,3,4 todo: ): https://gemini.google.com/app/5fe7cce3ffd63bc6
//* use wincoe.NewBoundProc instead of the usual NewProc
//fixedproperly//* current bug is: during resize releasing RMB won't run the corrected move-back because session is nil so a fixed-size unresizable window remains at the position it woulda been at if it resized! also if it doesn't hit this bug, you can still see it jump(now fixed too) during resizing attempts!
//* The Ratchet bug affecting Defraggler's main window ie. it would prevent shrinking after like the first 100ms due to W/H minimums being set due to procGetWindowRect getting the old size even tho the synchronous procSetWindowPos (that set the new size) did so.
//nolongerapplies//* maybe WindowMoveData should contain the knownMinW and knownMinH and resizeZone and startPt and startRect, so a dragState copy basically!
//yeah not setting it //* unclear if I should touch GOMAXPROCS at all though, maybe only if numCPU is less than 3 ?
//* shall we allow winkey+LMB+RMB to both work, currently just showing a console(or log) warning on MB UP, see if currentDrag should be different var for each, not same.
//* in ModeResize do the same 'if'-block that ModeMove has if already drag-moving
//*	trayIcon.HWnd = hwnd //doneFIXME: need to put this in a diff. variable so it doesn't depend on systray being inited! since it's used in other things!
//* move hideOverlay to wndProc away from hooks, hits stutter7 occasionally
//* move hideOverlay in wndProc
//they can be moved * see if child windows or dialogs like the LAN adapter config. can be moved and how.
//* FIXME: winkey+L will make prog think winkey is held after unlock!
//fixed printing was blocking thus our hooks weren't running //* ensure when runasadmin.bat and LMB-dragging scrollbar of that cmd.exe (admin) the mouse movements aren't 500ms delayed - probably need to use separate thread for hooks(+ their own msg.loop). This only happened in devbuild.bat not in build.bat btw.
//done//* tooltip not showing on warnings for lines in vscode, this was due to keyboard spamming keys (known keyboard bug)
//done//* while waiting for Enter key to exit, unregister mutex right before that.
//* native window dragging is broken currently, ie. if you uncheck "Manual move(no focus)" in systray that means native drag is used but it only works sometimes and it's pretty bad/choppy when it does work on cmd.exe due to the 1 thread of our program (for msg. loop and hooks, same thread) and cmd.exe does something (like selection started) which hangs our program thus the hooks too and Windows times out the hooks that's why it's not fully locked up.
//* currently native drag attempt on tcmd fails the first time then works 90% of the time but if tcmd then quits (not mid-drag, just normally when idle) then native drag on the newly started tcmd will fail 99.99% of the time!
//* native drag seems to need: attach thread, focus target window, send the SC_MOVE, send the WM_LBUTTONDOWN to prevent LListBox aka child from receiving it from the LMBDown that's next, then inject LMBDown because it needs the get async state for LMB to be DOWN! This works but not if you restart tcmd without also restarting winbollocks, which is positively odd.
//done//* give up on native drag due to so flakey, jesus; keep only manual drag.
//done4winkey//* winkey+L while dragging(ie. winkey+LMBdrag is down and dragging) will cause the subsequent winkeyUP and LMB UP to not be seen by my app due to being in lock screen when they happen, thus when unlocked, my app is still in drag mode, well winkeyUP is detected via get async state so it stops but LMB UP is never detected because LMB DOWN was eaten when it happened so can't query async state for LMB because it's never down during drag (got eaten and async state says it's thus UP) so while it stops due to winkey being UP if it is down for any reason it won't know to stop because LMB is physically UP in this case. So if we ever decide to allow the case when winkey+LMB starts the gesture but then we can allow winkeyUP to continue the dragging gesture until LMB is released(aka UP) then in this winkey+L happening during drag case, then unlocking session even tho LMB is UP the dragging continues as if it is still down!
//done//* ensure the focus via thread attach and focus via LMB are two systray menu options and not chained so the LMB doesn't click on items in start menu because it's the fallback and i know normal(thread attach) focusing fails for this case.
//done//* drags don't happen(are queued actually) when systray popup menu is open, likely due to single-threaded-ness! can test channel peak events this way (1179 got first time, 0 dropped tho). The previous implementation relied on PostThreadMessage to send a thread-level doorbell (WM_DO_SETWINDOWPOS) with HWND=0, which the modal message loop inside TrackPopupMenu would dequeue but then silently discard when calling DispatchMessage—because DispatchMessage ignores messages with a null HWND and does not invoke any window procedure for them; as a result, drainMoveChannel() never ran while the context menu was open, allowing move events to pile up unchecked in the buffered channel (thanks to the low-level mouse hook continuing to enqueue them), and only when a fresh drag gesture began after the menu closed did a new doorbell get posted and processed normally in the main GetMessage loop, triggering a massive single burst of backlog processing that made the window suddenly snap through every queued position. By switching to PostMessage targeted at your own hidden message-only window (trayIcon.HWnd ≠ 0), the doorbell becomes a proper window message; now, even during the modal popup, TrackPopupMenu’s internal loop dequeues it, recognizes it as non-menu-related, calls DispatchMessage, and your wndProc handles WM_DO_SETWINDOWPOS by draining the channel in near real-time, preventing any significant buildup and ensuring smooth, responsive window movement regardless of whether the systray context menu is open—thus eliminating the delayed flood behavior entirely while keeping the single-threaded Win32 architecture intact and avoiding re-entrancy risks in practice (since new WM_MYSYSTRAY triggers typically close the existing popup first).
//done(needed HIGH prio)//* when gemini replies there's mouse stuttering constantly but only when winbollocks is running, so maybe improve performance by doing something like this: "Inside mouseProc, do nothing but check the wParam and lParam. If it’s a move you care about, instantly send the coordinates into moveDataChan and return CallNextHookEx. Do the "heavy" logic (figuring out which window is under the mouse, calculating resize zones) in a different thread that reads from that channel." - gemini 3 fast.
//nevermind//* do LMB on systray enables/disables the hooks! because i notice mouse stuttering when gemini 3 Fast replies in firefox and it seems to happen only while my prog. is running which is unexpected!
//yes//* see if SW_RESTORE works for when window is maximized!
//done//* make sure hooks are on their own thread, different from msg. loop thread, to avoid lockups; use windows.GetCurrentThreadId() to check they're diff. threads; test with GOMAXPROCS=1; "you can force more threads via runtime.GOMAXPROCS(runtime.NumCPU())" and make sure it's at least 2 even if numCPU is less;
//done//* rate limit the resizing updates too! the move already has its systray limit option.
//done were too many resize attempts and setWindowPos is sync//* see why resizing empty notepad.exe window is stuttering and I can even not receive all the events, i see: New Channel Peak: 26 events queued (Dropped: 0); apparently because win11 times out my hook! Yeah it's because overlay update takes too long and my msg.loop and hooks are on same 1 thread, so any delay delays my mouse move hook and thus it causes win11 to timeout my hook and not send me any more events! // Low-level mouse hooks (WH_MOUSE_LL) are particularly sensitive to global system load and message pump health. Even if your hook body is fast, upstream congestion can still cause apparent drops.
//fixed 10k events spam in win event proc * find out why while searching/Find in regedit (as admin) window is open/running(finding) winbollocks does 8-10% cpu usage that's 1 full core and spins fans fast! also can't move that window during! and caused "New Channel Peak: 140 events queued (Dropped: 3)" and if trying to move any other windows they won't move and this happens "!! attempted to focus a windows with handle 0"
//* find out what happens when trying to move or resize the search mini window of regedit(admin) why things appear stuck and LMB has no effect, also logs seems to say hwnd is 0.
